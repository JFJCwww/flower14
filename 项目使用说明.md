# Flowers14 项目使用说明

本项目基于 YOLOv8 分类模型，实现 14 种花卉的自动识别分类。

---

## 一、环境准备

### 1.1 基础依赖

```
Python >= 3.8
PyTorch >= 2.0
ultralytics >= 8.4
PyQt5（仅可视化界面需要）
Pillow（数据准备需要）
```

### 1.2 安装依赖

```bash
pip install ultralytics PyQt5 Pillow
```

### 1.2 GPU 环境（可选，推荐）

如需使用 GPU 加速训练，请先安装对应版本的 CUDA 和 PyTorch：

```bash
# CUDA 11.8 示例
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
```

---

## 二、数据准备

### 2.1 数据来源

数据集来自 [ModelScope - flowers14](https://www.modelscope.cn/datasets/tany0699/flowers14)，包含 14 种花卉图片，共约 13740 张。

### 2.2 准备方式

运行数据准备脚本，自动完成下载、解压和格式转换：

```bash
python prepare_data.py
```

脚本支持三种数据来源（按优先级自动尝试）：

1. **zip 文件**：将 `train.zip` 和 `val.zip` 放入 `flowers14/` 目录
2. **已有图片目录**：直接使用 `flowers14/train/` 和 `flowers14/val/`
3. **ModelScope SDK**：需要安装 `modelscope` 包

准备完成后，数据以 YOLO 分类格式存放在 `flowers14_data/` 目录：

```
flowers14_data/
├── train/          # 训练集（约 13642 张）
│   ├── astilbe/
│   ├── bellflower/
│   └── ...
└── val/            # 验证集（约 98 张）
    ├── astilbe/
    └── ...
```

---

## 三、模型训练

提供两种训练方案：

| | 小型训练 `train_yolo.py` | 大型训练 `train_yolo_opt.py` |
|---|---|---|
| 适用场景 | 快速验证、资源有限 | 追求更高精度 |
| 模型 | yolov8n-cls（~3M 参数） | yolov8s-cls（~5M 参数） |
| 输入尺寸 | 224x224 | 384x384 |
| 批次大小 | 32 | 16 |
| 训练轮数 | 50（早停 patience=10） | 100（早停 patience=25） |
| 优化器 | auto | AdamW |
| 数据增强 | 默认 | randaugment + 更多增强 |
| GPU 显存需求 | ~2GB | ~4GB |

### 3.1 小型训练

```bash
python train_yolo.py
```

### 3.2 大型训练

```bash
python train_yolo_opt.py
```

### 3.3 训练结果

训练产出保存在 `runs/classify/` 目录下：

- `flowers14_cls/` — 小型训练结果
- `flowers14_cls_v2/` — 大型训练结果

每个目录下包含：
- `weights/best.pt` — 最佳模型权重
- `weights/last.pt` — 最后一轮模型权重
- 训练曲线图、混淆矩阵等

---

## 四、模型推理

### 4.1 命令行推理

```bash
# 单张图片
python predict.py test.jpg

# 多张图片
python predict.py img1.jpg img2.jpg

# 整个文件夹
python predict.py test_folder/
```

输出示例：

```
加载模型: runs/classify/flowers14_cls-5/weights/best.pt

共 3 张图片，开始预测:

  rose.jpg: 玫瑰 (rose) - 置信度 98.72%
  sunflower.jpg: 向日葵 (sunflower) - 置信度 95.31%
  tulip.jpg: 郁金香 (tulip) - 置信度 91.45%
```

> 如需使用大型训练的模型，修改 `predict.py` 中的 `MODEL_PATH` 为 `runs/classify/flowers14_cls_v2/weights/best.pt`。

### 4.2 可视化界面

```bash
python work.py
```

功能：
- 点击「开始识别」选择包含花朵图片的文件夹
- 自动轮播图片（每张 2 秒），实时显示识别结果
- 支持上一张 / 下一张手动翻页
- 支持暂停 / 继续播放
- 显示中文花名和置信度

---

## 五、花卉类别一览

| 编号 | 英文名 | 中文名 |
|------|--------|--------|
| 0 | astilbe | 落新妇 |
| 1 | bellflower | 风铃草 |
| 2 | black_eyed_susan | 黑眼菊 |
| 3 | calendula | 金盏花 |
| 4 | california_poppy | 金英花 |
| 5 | carnation | 康乃馨 |
| 6 | common_daisy | 雏菊 |
| 7 | coreopsis | 金鸡菊 |
| 8 | dandelion | 蒲公英 |
| 9 | iris | 鸢尾花 |
| 10 | rose | 玫瑰 |
| 11 | sunflower | 向日葵 |
| 12 | tulip | 郁金香 |
| 13 | water_lily | 睡莲 |

---

## 六、常见问题

### Q: 训练时提示 CUDA out of memory

减小 `batch` 参数值，例如将 32 改为 16 或 8。

### Q: Windows 下训练报多进程错误

确保脚本中 `workers=0`（已默认设置）。

### Q: 如何使用自己的数据集

按照 YOLO 分类格式组织目录结构，每个类别一个文件夹，然后修改 `flowers14.yaml` 中的路径和类别名。

### Q: 推理时找不到模型

确认已运行过训练，且 `runs/classify/` 下存在 `weights/best.pt` 文件。
